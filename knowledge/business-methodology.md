# 业务方法论

先拆业务，再映射技术。十维上下文必须覆盖：业务主体、业务对象、触发条件、输入数据与来源、业务规则、系统关系、数据处理与流转、输出结果与落点、约束条件、风险权限与审计。

五层拆解：L1 业务目标（指标与边界）；L2 业务场景（角色、触发、对象、结果）；L3 端到端流程（主线、分支、异常、审批）；L4 输入输出明确的独立任务；L5 可执行或调用工具的原子动作。AI、Agent、RPA、API 不是业务层级。
