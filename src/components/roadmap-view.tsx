import type { AnalysisResult } from "@/lib/schemas";
export function RoadmapView({ items }: { items: AnalysisResult["implementationRoadmap"] }) { return <div className="roadmap">{items.map(x=><div className="roadmap-item" key={x.phase}><div className="roadmap-phase">{x.phase}</div><div className="roadmap-body"><b>{x.goal}</b><div className="cards"><div><div className="mini-title">范围</div>{x.scope.join("；")}</div><div><div className="mini-title">交付物</div>{x.deliverables.join("；")}</div><div><div className="mini-title">验收标准</div>{x.acceptanceCriteria.join("；")}</div></div><div className="muted">依赖：{x.dependencies.join("；")}</div></div></div>)}</div>; }
